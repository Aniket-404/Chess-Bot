// Move history tracking and navigation
// This file will handle move history management

export const addMoveToHistory = () => {
  // Add move to history
  return [];
};

export const undoLastMove = () => {
  // Undo last move
  return null;
};
